# WIP: Prometheus exporter for Unity Licensing Server

```console
$ go build .
$ ./uls_exporter -h
Usage of ./uls_exporter:
  -listen string
        address to listen (default ":9101")
  -path string
        path to export metrics (default "/metrics")
  -uri string
        server base URI (default "http://localhost:8080")
```
